import * as React from 'react'
import { View } from '@tarojs/components'

const Counter: React.FC = () => {
	return (
		<View className='flex items-center justify-center text-white bg-red-500 h-160px w-3/7'>
			测试组件windi样式
		</View>
	)
}

export default Counter
